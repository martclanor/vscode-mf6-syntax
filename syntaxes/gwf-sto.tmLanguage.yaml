scopeName: source.mf6.gwf-sto
patterns:
  - include: 'source.mf6.common'
  - include: '#block'
  - include: '#keyword'
repository:
  block:
    patterns:
      - name: entity.name.type
        match: (?i)\b(OPTIONS|PERIOD|GRIDDATA)\b
  keyword:
    patterns:
      - name: variable.parameter
        match: (?i)\b(SAVE_FLOWS|STORAGECOEFFICIENT|SS_CONFINED_ONLY|TVS6|FILEIN|EXPORT_ARRAY_ASCII|EXPORT_ARRAY_NETCDF|DEV_ORIGINAL_SPECIFIC_STORAGE|DEV_OLDSTORAGEFORMULATION|ICONVERT|SS|SY|IPER|STEADY-STATE|TRANSIENT)\b
