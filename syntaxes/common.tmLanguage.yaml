scopeName: source.mf6.common
patterns:
  - include: '#comment'
  - include: '#reserved'
repository:
  comment:
    patterns:
      - name: comment.line
        match: (^|\s)(#|!|//).*$
  reserved:
    patterns:
      - name: entity.name.section
        match: ^\s*(?i)(BEGIN|END)\b
